{
  "name": "pie",
  "version": "1.0.0",
  "description": "",
  "main": "./dist/index.js",
  "dependencies": {
    "flow-bin": "^0.69.0",
    "flow-remove-types": "^1.2.3",
    "mustache": "^2.3.0",
    "ramda": "^0.25.0"
  },
  "devDependencies": {},
  "scripts": {
    "build": "flow-remove-types -a ./src/ -d ./dist/",
    "prepare": "npm run build",
    "typecheck": "flow check --all",
    "prepublishOnly": "npm test",
    "docs": "npm test && mash doc/README.md > ./README.md",
    "test-examples": "node doc/example/*.js > /dev/null",
    "pretest": "npm run build",
    "test": "node dist/index.spec.js && npm run typecheck && npm run test-examples"
  },
  "author": "",
  "license": "ISC"
}
